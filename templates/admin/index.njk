<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ data.admin.title }}</title>
    <link rel="stylesheet" href="/admin/assets/admin.css" />
  </head>
  <body class="min-h-screen flex flex-col p-6 gap-4">
    <header>
      <p class="text-sm uppercase tracking-widest text-slate-400">Admin</p>
      <h1 class="text-4xl font-extrabold">{{ data.admin.title }}</h1>
      <p class="text-slate-300">{{ data.admin.tagline }}</p>
    </header>
    <main class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <section class="admin-card">
        <h2 class="text-2xl font-semibold mb-2">Templates</h2>
        <p>Les pages se trouvent dans <code>templates/admin</code>.</p>
      </section>
      <section class="admin-card">
        <h2 class="text-2xl font-semibold mb-2">Données</h2>
        <p>Les contenus proviennent de <code>data/*.json</code>.</p>
      </section>
    </main>
    <script src="/admin/assets/admin.js" defer></script>
  </body>
</html>

